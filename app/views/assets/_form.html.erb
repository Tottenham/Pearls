<%= form_for(@asset) do |f| %>
  <% if @asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@asset.errors.count, "error") %> prohibited this asset from being saved:</h2>

      <ul>
      <% @asset.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :girl %><br />
    <%= f.collection_select :girl_id, @girls, :id, :name, {:prompt => 'Select Girl'} %> 
  </div>

  <div class="field">
  	<%= f.label :image %><br />
    <%= f.file_field :image %><br />
  </div>         
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>